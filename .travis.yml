sudo: required

language: node_js

services:
  - docker

script:
  - docker build -t capcodigital/ideafunnel dev/app/

after_success:
  - docker login -u $DOCKER_USER -p $DOCKER_PASSWORD
  - docker tag if-app capcodigital/ideafunnel:latest
  - docker push capcodigital/ideafunnel:latest
